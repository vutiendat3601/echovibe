import { ArtistNationalityPipe } from './artist-nationality.pipe';

describe('ArtistNationalityPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistNationalityPipe();
    expect(pipe).toBeTruthy();
  });
});
