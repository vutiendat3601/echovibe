apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  namespace: {{ .Values.global.namespace }}
spec:
  type: ExternalName
  externalName: auth.echovibe.io.vn
  ports:
    - name: http
      protocol: TCP
      port: 443
      targetPort: 443
