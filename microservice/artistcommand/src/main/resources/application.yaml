server:
  port: 6100
management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    env:
      enabled: true
spring:
  application:
    name: artistcommand
  docker:
    compose:
      enabled: false
springdoc:
  api-docs:
    path: /openapi
  swagger-ui:
    path: /openapi.html
